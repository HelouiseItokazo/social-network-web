export default class User {

    constructor(uid, name, password, urlPhoto){
        this.uid = uid,
        this.name = name,
        this.password = password,
        this.urlPhoto = urlPhoto
    }//endConstructor

}//endClass